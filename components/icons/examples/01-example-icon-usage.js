import React, { Component } from 'react';
import {
  CloseIcon
} from '../src/index';

export default class App extends Component {
  render() {
    return (
      <div>
        <CloseIcon />
      </div>
    );
  }
}
